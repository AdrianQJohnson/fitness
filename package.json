{
  "name": "fitness",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "if-env TYPE=server && (cd server && npm install) || (cd client && npm install)",
    "server": "node ./sever/server.js",
    "client": "cd client && npm start"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/AdrianQJohnson/fitness.git"
  },
  "dependencies": {
    "if-env": "^1.0.4"
  }
}